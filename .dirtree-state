# Dirtree: Stateful directory trees in the CLI for humans and LLMs. https://github.com/pmarreck/dirtree
ver=1.2
depth=3

annotate=[
	.github/workflows/ci.yml = GitHub Actions gate for every flake check plus the complete repository test suite
	CODE_REVIEW.md = Thirteen-dimension audit with severity-ranked findings, exact source references, and tracked dispositions.
	FIDELITY_GAP_MATRIX.md = Source-to-WAT implementation ledger, deliberate deviations, remaining fidelity gaps, and completion gate
	GAMEPLAY_DESIGN_RESEARCH.md = Sourced Asteroids and Blasteroids engagement analysis, scope guards, prioritized improvements, and falsifiable playtests
	MEMORIES = Project-specific durable lessons discovered during the spike
	MEMORIES/Complete test suites should enter a pure flake environment = Durable lesson preventing global PATH tools from masking undeclared test dependencies.
	MEMORIES/Decimal fixed point in WAT needs a sealed float adapter and rescaled collision squares = Project lesson on decimal millionths, one-way GPUI conversion, and overflow-safe collision tests
	MEMORIES/Grow snapshotted fixed pools with tail storage and disjoint draw IDs.frontmatter.md = Project lesson for extending fixed snapshot pools without moving legacy records or colliding draw IDs.
	MEMORIES/Hot reload must clear edge-latched input while preserving persistent toggles.frontmatter.md = Project lesson: post-restore clears held input edges but retains persistent modes
	MEMORIES/Keep application ABI checks self-contained when the host checkout is not pinned = Durable guidance for reproducible downstream ABI conformance checks.
	MEMORIES/Port per-second browser physics to fixed ticks by converting dimensions exactly once = Dimensional-analysis lesson for velocity, acceleration, rotation, and strict time thresholds in 60-Hz fixed simulation
	MEMORIES/Rational display-following needs separate guest and host controls = Separates guest WAST selector assertions from independently controlled host rational-rate adoption tests
	MEMORIES/Snapshot mutable target pools before piercing attacks so spawned children survive the same beam = Why piercing laser collision snapshots active asteroid slots before split mutations
	MEMORIES/Swept collision on a wrapping world must reject false cross-screen segments.frontmatter.md = Project lesson: reject false cross-screen sweeps when projectile endpoints wrap
	MEMORIES/WAT applications should keep behavior tests in companion WAST scripts = WAST application-test ownership, instrumented-host composition, independent Rust controls, and structural-lint boundary
	MEMORIES/WAT subtraction negates a value only when zero is pushed before it = Records noncommutative WAT operand order and the local-space thruster regression.
	PLAN.md = Active work, deep-review resolution status, and recent green milestones; older history remains in Git.
	PROJECT_OVERVIEW.md = vibesteroids_wat ownership boundary, dependency relationship, status, and terminology
	README.md = Public vibesteroids_wat architecture, hyphenated commands, controls, live-edit proof, status, and tests
	RULES.md = Non-negotiable WAT ownership, fixed-point, WAST, determinism, Nix, and gameplay-change rules
	SPEC.md = Application contract: Aedicule lifecycle, schema-9 32 KiB snapshot, fixed-point timing, rendering, inputs, and deterministic limits.
	VIBESTEROIDS_BEHAVIOR_SPEC.md = Source-derived gameplay algorithms and quirks, including the 256-slot split player-projectile pool.
	build = Top-level optimized Nix build and test/debug dispatcher
	code.wat = Schema-9 deterministic fixed-point Vibesteroids guest at 120 Hz, including named flag/synth boundaries, precomputed bullet lifetimes, hazardous blasts, and pure vector rendering.
	docs/help-and-powerup-hud-concept.svg = Proposed two-column Help and active laser/rapid-fire countdown badges awaiting Peter's visual approval.
	docs/voyager-satellite-concept.svg = Voyager-derived derelict-satellite concept shown enlarged and at honest gameplay scale for Peter's visual approval.
	flake.nix = Pinned aedicule input, data-only WAT artifact, hyphenated command wrappers, checks, and tools
	run = Nix launcher for the composed Vibesteroids Aedicule package
	test = Pure complete WAST, WAT-policy, naming, repository-boundary, flake-composition, and shell test accumulator
	tests/cli/abi_namespace = Set classifier forbidding mixed legacy/new ABI namespaces and requiring the complete aedicule.v0 AE_* surface
	tests/cli/aedicule_abi_contract = Self-contained classifier for documented Aedicule ABI and snapshot contract drift.
	tests/cli/aedicule_runtime = Locked-host integration gate requiring production WAT to configure, tick, and render through Aedicule without stderr
	tests/cli/aedicule_runtime_portability = Isolated classifier proving the locked-host gate runs without private dotfiles and propagates failures.
	tests/cli/development_dependencies = Classifier for every external command required by the pure application test shell
	tests/cli/flake_composition = Nix regression proving the wrapper composes distinct WAT and pinned-frontplane derivations
	tests/cli/naming_conventions = Classifier enforcing underscore project identifiers and hyphenated command names
	tests/cli/repository_boundary = Set classifier forbidding native frontplane paths while permitting application-owned fixtures
	tests/cli/run_from_any_directory = CLI regression proving ./run anchors the flake when invoked outside the project directory
	tests/cli/run_wast_diagnostics = Failure-path classifier proving WAST composition gates execution and rejected suites remain inspectable.
	tests/fixtures/run-bin/nix = Fake nix executable that classifies launcher arguments without starting the GUI
	tests/fixtures/runtime-bin/nix = Fake Nix adapter for portable runtime-gate success and failure controls.
	tests/fixtures/wast-bin/awk = Controllable Awk adapter for WAST composition-failure tests.
	tests/fixtures/wast-bin/wasmtime = Controllable Wasmtime adapter for WAST execution-failure tests.
	tests/lint-wat = Structural classifier enforcing one sealed float adapter and integer-only gameplay WAT
	tests/run-wast = RAM-composes production WAT at four rational rates, optionally retaining a failed suite for diagnosis, then runs all WAST proofs.
	tests/wast/aedicule-contract.wast = WAST proof of mandatory Aedicule v0 lifecycle plus accept/reject controls for frame-global stable draw IDs
	tests/wast/aedicule-v0.wast = Instrumented aedicule.v0 fake host with frame-wide stable-ID validation and value classifiers for draw, blast, text, audio, and synth effects
	tests/wast/enemy-and-powerup.wast = Deterministic WAST scenarios for UFO combat/blasts, package pickup/destruction, scheduling, and laser behavior.
	tests/wast/equal-time.wast = Cross-rate production proofs for exact collision-free projectile travel and bounded one-second damping at 60/120 Hz.
	tests/wast/gameplay.wast = Source-derived deterministic gameplay and rendering behavior assertions, including swept hits and Death Blossom UI/input.
	tests/wast/host-v0.wast = Deterministic instrumented WAST implementation of host.v0 for guest-emitted render, menu, synth, audio, and effect assertions
	tests/wast/rational-timing.wast = Guest contract fixtures for rational display refresh events and AE_tick_rate selector responses
	tests/wast/vibesteroids.wast = Public-ABI probe module for AE_* lifecycle, state, game configuration, render, resize, and rate assertions
]
